import st from './Header.module.css';

const Header = () => {
  return (
    <div className={st.mainDiv}>
      <h1 className={st.header_caption}>Some App</h1>
    </div>
  );
};

export default Header;
